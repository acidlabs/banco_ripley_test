module.exports = {
  displayable: ({ displayable }) => displayable,
  id: ({ id }) => id,
  identifier: ({ identifier }) => identifier,
  name: ({ name }) => name,
  usage: ({ usage }) => usage,
  value: ({ value }) => value
};
